# Screenshots

Drop UI screenshots here so they render in the main README:

- `brief.png` — the Brief view (market strip + four cards + sources)
- `ask.png` — the Ask view (question + referenced answer)

Quick way to capture: run the backend and frontend, search for a coin (e.g. `XRP`),
take a screenshot, and save it with the names above.
